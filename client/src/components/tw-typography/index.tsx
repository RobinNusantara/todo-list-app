import React from 'react';

interface Props {
  className: string;
  children: string;
}

const Subtitle: React.FC<Props> = ({children}) => {
  return <p></p>;
};

const Body: React.FC<Props> = ({children}) => {
  return <p></p>;
};

const Header: React.FC<Props> = ({children}) => {
  return <p></p>;
};


export default {Subtitle, Body, Header};
